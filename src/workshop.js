import {findValidator} from "./validators/validator";

export class Workshop {
    constructor(name) {
        this.Name = name;
        this.Creators = {};
    }
    
    register(objName, fn, validators) {
        
    }
    
    validate(objName, args) {
        
    }
    
    create(objName, args) {
        
    }
    
    parse(line) {
        
    }
}